

#include "pharm.h"

size_t ranking_dev(struct* ordering)
{
        struct** ordering ord = &ordering;
        size_t dev = 0;

        while(*ord){
                dev += ((*ord)->

        }

        return dev;

}


